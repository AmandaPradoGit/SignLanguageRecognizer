# SignLanguageRecognizer
Sistema para reconhecimento de gestos em libras, utilizando a biblioteca mediapipe do python

## Instalações Necessárias<br>
```python3 -m pip install opencv-python mediapipe scikit-learn pandas joblib```<br><br>
```pip install opencv-python mediapipe scikit-learn pandas joblib```<br><br>
```pip install mediapipe opencv-python tensorflow numpy```<br><br>
```sudo apt install python3-venv python3-full```<br><br>
```sudo apt install python3-xyz```<br><br>
```pip install mediapipe opencv-python tensorflow scikit-learn numpy pandas```<br><br>
```sudo apt install python3-pip```<br><br>
```pip install seaborn matplotlib ```